/**
 * 基本设置.js
 */
define(function(require, exports, module){
	// 引入js和css区域
		var $ = require('jquery');
		var util = require('util');
		
		// 变量区域
		
		// 初始化区域
		function _init(){
			initconfig();
			initEvent();
		}
		
		//初始化配置
		function initconfig(){
			
		}
		
		//初始化事件
		function initEvent(){
			
		}
		
		
		//业务逻辑
		
		
		
		// 输出区域
		exports.init = _init;
})